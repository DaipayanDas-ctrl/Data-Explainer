fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic>=2.9.2
python-dotenv==1.0.1

# only one of these two is actually required at runtime, based on LLM_PROVIDER
# in .env — but installing both is harmless and saves a re-install if you switch.
anthropic==0.34.2
groq>=0.9.0
boto3>=1.28.0
slowapi>=0.1.9
pyjwt>=2.8.0
mangum>=0.17.0


